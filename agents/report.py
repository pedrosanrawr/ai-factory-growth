"""
Agent 8: Report

Groupmate owner:
- Read the ranked records
- Create display-ready output for the app or export
- Fill `primary_risk` and `status`
- Return the final records and any summary text needed by the UI

Suggested function shape:

def run(records: list[dict], risk_discount_pct: float = 10.0,
        power_efficiency_weight: float = 1.2) -> tuple[list[dict], str]:
    ...
"""
