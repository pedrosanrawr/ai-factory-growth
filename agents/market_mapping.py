"""
Agent 1: Market Mapping

- Read each record's `role`
- Match it against `schema.SEGMENT_WEIGHTS`
- Write the result into `segment_weight`
- Return the updated `list[dict]`

Suggested function shape:

def run(records: list[dict]) -> list[dict]:
    ...
"""
