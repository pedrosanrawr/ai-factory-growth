"""
Agent 7: Ranking

- Read the completed records from the earlier agents
- Compute `tafgs_score`
- Sort based on the selected ranking priority
- Fill the `rank` field
- Return the ranked `list[dict]`

Suggested function shape:

def run(records: list[dict], ranking_priority: str = "TAFGS Score") -> list[dict]:
    ...
"""
